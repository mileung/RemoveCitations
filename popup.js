document.addEventListener('DOMContentLoaded', function() {
  var button = document.getElementById('button');
  var action = document.getElementById('action');
  // storage.sync.get('Action', function(data) {
  //   action.innerHTML = data.action;
  // });
  //
  // button.addEventListener('click', function() {
  //   if (action.innerHTML === 'Disable') {
  //     storage.sync.set({'Action', 'Disable'});
  //     action.innerHTML = 'Enable';
  //   } else {
  //     storage.sync.set({'Action', 'Enable'});
  //     action.innerHTML = 'Disable';
  //   }
  // });
});
